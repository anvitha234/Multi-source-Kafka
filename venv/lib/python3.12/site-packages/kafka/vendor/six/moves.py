# moves.py: expose names from six.moves so kafka.vendor.six.moves.<name> works.
try:
    from six.moves import *   # noqa: F401,F403
except Exception:
    # Minimal fallback: provide range only
    class _Moves:
        range = range
    range = _Moves.range
